<Window x:Class="TypoZap.HotkeyRecordingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Record Hotkey"
        Height="250"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="Record New Hotkey"
                   FontSize="18"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>
        
        <!-- Instructions -->
        <TextBlock Grid.Row="1" 
                   Text="Press any combination of keys to record a new hotkey:"
                   FontSize="12"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>
        
        <!-- Hotkey Display -->
        <Border Grid.Row="2" 
                BorderBrush="Blue" 
                BorderThickness="2" 
                Background="LightBlue" 
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,15">
            <TextBlock x:Name="HotkeyDisplayTextBlock"
                       Text="Press keys..."
                       FontSize="16"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>
        
        <!-- Spacer -->
        <Grid Grid.Row="3" />
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            
            <Button x:Name="ClearButton"
                    Content="Clear"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="ClearButton_Click"/>
            
            <Button x:Name="SaveButton"
                    Content="Save"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="SaveButton_Click"/>
            
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="80"
                    Height="30"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
